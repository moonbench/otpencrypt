[package]
name = "otpencrypt"
version = "0.2.1"
edition = "2021"
description = "Encrypts and decrypts files with one-time pads"
license = "GPL-3.0-only"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
libc = "0.2"
rand = "0.8"
zeroize = "1"
